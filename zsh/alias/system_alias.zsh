alias c=cd
alias s=ls
alias sa="ls -a"
alias R="rm -rf"
alias cl=clear
alias lc=clear

# xclip must be installed
# how do to use: `cat some-file.txt | copy` than you can just ctrl+v the some-file.txt content
alias copy="xclip -selection clipboard"
